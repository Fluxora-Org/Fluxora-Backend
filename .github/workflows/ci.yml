name: CI

on:
  push:
    branches: [main, develop, 'feature/**']
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    name: Lint, Format, Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run pnpm audit (high/critical fail)
        run: pnpm audit --audit-level=high --json > audit.json || true

      - name: Parse audit results and comment on PR
        run: node .github/workflows/audit-comment.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number || '' }}

      - name: Check formatting and lint
        run: pnpm run lint

      - name: Type check
        run: pnpm run typecheck

      - name: Format check
        run: pnpm run format:check

      - name: Build
        run: pnpm run build

      - name: Run tests
        run: pnpm test

  docker-build:
    name: Build & Smoke Test Docker Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t fluxora-backend:ci-test .

      - name: Start Container
        run: docker run -d -p 3000:3000 --name fluxora-test-container fluxora-backend:ci-test

      - name: Smoke Test Health Check
        run: |
          echo "Waiting for container to report healthy..."
          for i in {1..12}; do
            STATUS=$(docker inspect -f '{{.State.Health.Status}}' fluxora-test-container)
            if [ "$STATUS" == "healthy" ]; then
              echo "Container is healthy!"
              exit 0
            fi
            sleep 5
          done
          echo "Container failed to become healthy. Fetching logs..."
          docker logs fluxora-test-container
          exit 1